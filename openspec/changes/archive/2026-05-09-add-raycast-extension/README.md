# add-raycast-extension

Build a separate Raycast extension that mirrors the Git Manager's repo workspace inside Raycast: repo picker, branches/worktrees list, per-row actions (commit, push, pull, sync, open in editor).
